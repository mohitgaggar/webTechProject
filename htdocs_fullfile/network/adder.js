
function add(domain)
{
    if(domain==0)
    {
        var link=prompt('Add E-Books or PDFs link');
    }
    else if(domain==1)
    {
        var link=prompt('Add Websites or Blog Link');
    }
    else if(domain==2)
    {
        var link=prompt("Add Videos or Webinars link");
    }
    else if(domain==3)
    {
        var link=prompt("Add Online Platforms and Application Software Download link");
    }
}
